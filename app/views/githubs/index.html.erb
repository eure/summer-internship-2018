<h1>Githubs#Index</h1>
<p>Find me in app/views/githubs/developers.html.erb</p>

<% @contents.each do |content| %>
  <%= link_to("https://github.com#{content[:href]}") do %>
    <div class="media text-muted box">
      <%= content.inner_text %>
    </div>
  <% end %>
<% end %>